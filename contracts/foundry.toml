[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
# allow Deploy.s.sol to write the deployments manifest the SDK reads
fs_permissions = [{ access = "read-write", path = "./deployments" }]

[fmt]
line_length = 100
