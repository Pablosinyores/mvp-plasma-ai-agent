# Backend (FastAPI) base URL. The WS URL is derived from this unless VITE_WS_URL is set.
VITE_API_BASE=http://localhost:8080
# Optional explicit WebSocket URL (defaults to ws(s)://<API_BASE host>/ws)
# VITE_WS_URL=ws://localhost:8080/ws
