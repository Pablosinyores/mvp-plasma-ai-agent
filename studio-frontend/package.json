{
  "name": "plasma-studio-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Plasma Agent Studio — standalone React control-plane frontend. Talks to the FastAPI backend (default http://localhost:8080) over REST + WebSocket.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "viem": "^2.53.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
